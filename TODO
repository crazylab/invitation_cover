Write Library to print(represent) label from record:

List of records provided
    Format: firstName,lastName,gender,thresholdAge,city,state,country

Furnish label options for specific country

Output:
    Mr/Ms First Last, Country
    Mr/Ms Last, First, Country

    Prefix :
        Male -> Mr
        Female -> Ms

------------------------------------------------------------------
read command line input as followes:
    InvitationGenerator --firstNameFirst --country country_name --ageabove thresholdAge file_path
    InvitationGenerator --lastNameFirst --country country_name --ageabove thresholdAge file_path